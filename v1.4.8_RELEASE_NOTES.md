# Novel Studio v1.4.8

## 현재 상태 스냅샷 UI 개선

- 우측 현재 상태 스냅샷은 기존 설계대로 `state:현재화-1`을 기준으로 표시합니다.
- 우측 패널 상단에 `현재 작성 화`와 `기준 스냅샷 화`를 명시합니다.
- AI가 생성한 `[사건]`, `[인물]`, `[경지]`, `[위치]`, `[시간]`, `[소지품]`, `[관계]`, `[복선]`, `[미해결]`, `[다음 연결]` 등의 필드 태그를 자동으로 분리해 카드 형태로 표시합니다.
- 필드명은 굵게 별도 영역으로, 실제 상태 문장은 본문 영역으로 분리해 가독성을 높였습니다.
- 필드 태그가 없는 AI 응답도 `상태 요약` 카드로 안전하게 표시합니다.
- 원고 저장 후 장기 기억 갱신 콜백에서 우측 스냅샷 패널을 다시 갱신합니다.
- 기존 스냅샷 저장/조회 데이터 구조와 `state:N-1` 기준은 유지합니다.

## 검증

- Python 전체 모듈 문법 검사 통과
- 기존 pytest: 3 passed
