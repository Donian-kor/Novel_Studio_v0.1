# Novel Studio v1.1 변경사항

## 핵심 수정

### 화별 플롯 생성 안정화
- AI 출력의 화 헤더를 `### 제1화`, `제1화 - 제목`, `1화. 제목`, `[화 번호]: 1화` 등 여러 형태로 파싱.
- `제목:` / `[제목]:` 별도 필드 인식.
- 요청 범위의 모든 화가 실제로 생성·파싱되었는지 검증.
- 누락 화가 있으면 1회 보완 생성.
- 보완 후에도 누락이 있으면 DB에 부분 저장하지 않고 원인과 AI 응답 일부를 오류로 표시.
- 화별 플롯 생성 전 전체 플롯과 대상 스토리 구간 존재 여부를 검사.

### 500화 지원
- 화별 플롯 시작/끝 번호 입력 최대값을 5000으로 확대.
- 전체 화별 플롯 생성 버튼이 프로젝트 총 화수까지 선택 가능.

### Plan Contract 잠금
- 잠긴 핵심 기준은 편집창을 읽기 전용으로 전환.
- 잠긴 상태에서 저장/수정 시도를 차단.
- 화면을 다시 열어도 잠금 상태를 복원.

### 유지보수성
- `PlanningView`의 중복 property 정의 제거.
- 버전 표기를 1.1로 통일.
- 존재하지 않는 `run_windows.bat`에 대한 README 실행 안내를 제거하고 실제 수동 실행 절차와 일치시킴.
- 화별 플롯 파서 회귀 테스트 4건 추가.

## 검증
- Python `compileall` 통과.
- 파서 회귀 테스트 4건 모두 통과.
- 현재 리뷰 실행 환경에는 PySide6가 설치되어 있지 않아 실제 GUI 부팅 테스트는 수행하지 못함.
