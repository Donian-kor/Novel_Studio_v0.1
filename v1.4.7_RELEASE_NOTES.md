# Novel Studio v1.4.7

## 수정
- 우측 `현재 작품 상태` 패널이 `현재 화 - 1`만 조회하던 문제 수정
- 현재 화의 확정 상태 스냅샷(`state:N`)을 우선 표시
- 현재 화 스냅샷이 없으면 직전 화 확정 스냅샷으로 자동 대체
- 원고 저장 직후 우측 상태 패널 즉시 갱신
- 원고 저장 후 장기기억/연속성 갱신이 완료되면 현재 화 스냅샷으로 자동 갱신
- 상단 대시보드도 저장 후 함께 갱신

## 검증
- `python -m compileall -q novel_studio main.py` 통과
- `PYTHONPATH=. pytest -q` → 3 passed
