# Novel Studio v1.4.9

## AI 작업 스냅샷 / 화면 전환 안전성

- AI 스트리밍 작업마다 프로젝트 `temp/ai_snapshots/`에 중간 결과를 자동 저장한다.
- 화면/메뉴 전환과 AI 작업의 생명주기를 분리했다.
- 원고 AI 집필은 시작 시 화 번호를 고정하고, 완료 결과를 고정된 화의 `chapters/###.txt`와 DB 메타에 즉시 저장한다.
- AI 집필 중 다른 화를 선택해도 생성 결과가 새로 선택한 화에 섞이지 않는다.
- AI 집필 중 대상 화로 돌아오면 진행 중인 메모리 버퍼를 다시 표시한다.
- 화 전환 직전에 현재 원고의 임시 초안을 저장한다.
- AI 집필 결과는 화면 저장 버튼을 기다리지 않고 `초안` 상태로 자동 보존한다.
- 스트리밍 AI 작업의 성공/실패/취소 시 스냅샷 상태를 기록한다.
- `chapterList` 갱신 시 선택 변경 시그널을 잠시 차단해 불필요한 화 재로딩을 방지한다.

## 목적

AI가 아이디어/기획/플롯/스토리 구간/원고를 생성하는 동안 다른 메뉴로 이동해도 진행 결과가 UI에 종속되어 사라지지 않도록 하는 것이 핵심이다.
