# Novel Studio v1.4.6

## 이번 버전의 핵심

v1.4.5 코드 점검에서 확인된 기능 연결 오류와 구조 문제를 수정하고, v1.4.0 설계 원칙에 맞춰 핵심 AI 작업 흐름을 Controller → Service → Core → DB 구조로 정리했다.

## 주요 수정

- AI 집필·윤문·연속성 검사 스트리밍/완료 callback 연결 수정
- Controller가 현재 작업과 취소 요청을 실제로 관리하도록 수정
- 스트리밍 작업 취소 시 다음 토큰 수신 지점에서 중단하도록 보완
- 장편 정밀 연속성 검사 입력을 실제 플롯·확정 상태·원고 기준으로 보강
- ThreadSafeDatabase의 단일 DB 연결 구조 정리
- Provider 스트리밍 재시도 정책 정비
- 엔티티 편집 저장을 선택 항목 중심으로 변경하고 이름 변경 처리 수정
- 확정 저장 전에 장기기억/연속성 AI 갱신을 자동 실행하지 않도록 변경
- ServiceFactory를 실제 애플리케이션 구성 경로에서 사용
- DB 및 스트리밍 재시도 회귀 테스트 추가
- 프로그램 버전을 1.4.6으로 통일

## 검증

- Python AST/compileall 검사 통과
- DB CRUD/연속성 조회 테스트 통과
- 스트리밍 재시도/HTTP 4xx 비재시도 테스트 통과

실행 환경에 PySide6가 설치되어 있지 않은 검토 환경에서는 실제 GUI 렌더링 테스트는 수행하지 못했다. 배포 환경에서는 `requirements.txt` 설치 후 GUI 실행 검증이 필요하다.
